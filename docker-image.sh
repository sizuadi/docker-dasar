# melihat list image pada docker kita
docker image ls

# mendownload image yang ada pada docker hub, aplikasi:tag-versi
docker image pull redis:latest

# menghapus docker image yang sudah didownload
docker image rm redis:latest
